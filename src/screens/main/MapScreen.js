import { common } from "../../components/common";

const { MainContainer, TextRobotoRegular } = common;

export default MapScreen = (props) => {
  return (
    <MainContainer>
      <TextRobotoRegular>MapScreen</TextRobotoRegular>
    </MainContainer>
  );
};